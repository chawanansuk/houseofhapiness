# คู่มือติดตั้งระบบหลังบ้าน — House of Happiness

> **อัปเดตสคริปต์เป็นเวอร์ชันใหม่ (v4):** เปิด Apps Script เดิม ลบโค้ดทั้งหมด
> → วางโค้ดใหม่จากไฟล์นี้ทับ → ใส่ TOKEN เดิม → กด Run `setupTriggers` อีก 1 ครั้ง
> **แล้วรัน `repairMissingDates` ด้วย 1 ครั้ง** — มันจะไล่อีเมลเก่าที่สแกนไปแล้ว
> มาเติม "วันเช็คอิน" ให้แถวที่วันที่ยังว่าง (อ่านจากหัวข้ออีเมล เช่น
> "...วันจันทร์ที่ 10 สิงหาคม ค.ศ. 2026" — เวอร์ชันก่อนอ่านรูปแบบ "ค.ศ." ไม่ได้)
> ของใหม่ v4: อ่านวันที่จากหัวข้ออีเมลเป็น fallback + จับคู่อีเมลยกเลิกแบบเทียบตัวเลข
> + ฟังก์ชันซ่อมข้อมูลย้อนหลัง + ชีต **Expenses** (บันทึกรายจ่าย/กำไร — สร้างให้อัตโนมัติ
> ครั้งแรกที่ใช้แท็บ "รายรับ-รายจ่าย" ในหลังบ้าน) | v3: สรุปงานเช้า 08:00 + สถานะ "รอเติมชื่อจาก Pulse"
> อย่าลืมตั้ง Time zone ของโปรเจกต์เป็น Bangkok: ⚙️ Project Settings → Time zone

ระบบนี้ทำให้คุณเห็น **ใครเข้าพักวันไหนบ้าง** ที่หน้า
**https://houseofhapiness.vercel.app/admin** โดยข้อมูลไหลเข้ามาอัตโนมัติจาก 2 ทาง:

1. **อีเมลยืนยันการจองจาก Booking.com** → Google Apps Script อ่านจาก Gmail ทุก 30 นาที → ลง Google Sheets
2. **คำขอจองตรงจากหน้าเว็บ** (booking.html) → บันทึกลงชีตเดียวกันทันที

> ยังไม่ได้ตั้งค่า? หน้า /admin จะอยู่ "โหมดตัวอย่าง" — เข้าดูด้วยรหัส `demo1234`
> เพื่อลองเล่น UI ได้ก่อน ข้อมูลที่เห็นเป็นข้อมูลสมมติ

---

## ขั้นที่ 1 — สร้าง Google Sheets + วางสคริปต์ (ประมาณ 10 นาที)

1. เข้า [sheets.new](https://sheets.new) ด้วยบัญชี **houseofhapinessbangkok@gmail.com**
   (บัญชีที่รับอีเมลจาก Booking.com) แล้วตั้งชื่อไฟล์ เช่น `HOH Bookings`
   > สำคัญ: อีเมลยืนยันการจองจาก Booking.com ต้องเข้ากล่องนี้ — ตั้งใน Extranet
   > (เมนู Account → Contacts) หรือตั้ง Outlook เดิมให้ส่งต่ออัตโนมัติมาที่อีเมลนี้
2. เมนู **Extensions → Apps Script** จะเปิดหน้าเขียนสคริปต์
3. ลบโค้ดเดิมทั้งหมด แล้ววางโค้ดจากไฟล์ **`backoffice/apps-script.gs`** ในรีโปนี้
4. บรรทัดบนสุด แก้ `var TOKEN = "เปลี่ยนรหัสลับตรงนี้";`
   → ตั้งรหัสลับยาว ๆ ของคุณเอง เช่น `hoh-2569-xK9mPq` (จดไว้ ใช้ในขั้นที่ 3)
5. กด 💾 บันทึก

### เปิดใช้ Web App (ให้เว็บไซต์คุยกับชีตได้)

6. กดปุ่ม **Deploy → New deployment**
7. กดไอคอนเฟือง เลือกประเภท **Web app** แล้วตั้งค่า:
   - Execute as: **Me**
   - Who has access: **Anyone**  ← จำเป็น (ความปลอดภัยอยู่ที่ TOKEN ไม่ใช่หน้านี้)
8. กด **Deploy** → อนุญาตสิทธิ์ที่ระบบถาม → **คัดลอก URL** ที่ลงท้ายด้วย `/exec` (จดไว้)

### เปิดการสแกนอีเมลอัตโนมัติ

9. ในหน้า Apps Script เลือกฟังก์ชัน **`setupTriggers`** จากเมนูด้านบน แล้วกด **Run**
   (อนุญาตสิทธิ์ Gmail ที่ระบบถาม — สคริปต์อ่านเฉพาะอีเมลจาก booking.com เท่านั้น)
10. ทดสอบทันทีได้โดยเลือกฟังก์ชัน **`testScanNow`** แล้วกด Run → เปิดชีตดู
    ถ้ามีอีเมลจองจาก Booking.com ใน 7 วันที่ผ่านมา จะเห็นแถวข้อมูลปรากฏ

> อีเมลที่อ่านแล้วจะถูกติดป้าย `HOH-บันทึกแล้ว` ใน Gmail จะได้ไม่บันทึกซ้ำ
> อีเมลที่อ่านไม่สำเร็จจะถูกบันทึกเป็นแถว "ตรวจสอบเอง" — ไม่มีการจองหายเงียบ ๆ

---

## ขั้นที่ 2 — ตั้งค่าบน Vercel (ประมาณ 5 นาที)

เข้า [vercel.com](https://vercel.com) → โปรเจกต์ **houseofhapiness** → **Settings → Environment Variables**
แล้วเพิ่มทีละตัว (Environment เลือก **Production**):

| Name | Value |
|---|---|
| `ADMIN_PASSWORD` | รหัสผ่านเข้าหน้า /admin (ตั้งเอง ยาว ๆ เดายาก) |
| `STAFF_PASSWORD` | *(ไม่บังคับ)* รหัสสำหรับพนักงาน — เข้าหน้า /admin ได้ เห็นผังห้อง/กดงานได้ครบ แต่**มองไม่เห็นตัวเลขเงิน** (รายได้/ยอดจอง ถูกตัดตั้งแต่เซิร์ฟเวอร์) และแก้ยอดเงินไม่ได้ |
| `SHEET_WEBAPP_URL` | URL `/exec` ที่คัดลอกจากขั้นที่ 1 ข้อ 8 |
| `SHEET_TOKEN` | รหัสลับเดียวกับ `TOKEN` ในสคริปต์ (ขั้นที่ 1 ข้อ 4) |

จากนั้นกด **Deployments → เมนู ⋯ ของรายการล่าสุด → Redeploy** เพื่อให้ค่าใหม่มีผล

### (สำคัญมาก) เชื่อมปฏิทิน iCal ของ Booking.com

อีเมลของ Booking.com ยุคใหม่**ไม่บอกวันที่เข้าพักครบถ้วน** — iCal คือแหล่งวันที่จริง
ถ้าไม่เชื่อม ตัวเลข "ห้องว่าง" หน้าเว็บจะไม่แม่น (หลังบ้านจะขึ้นแบนเนอร์ส้มเตือนจนกว่าจะตั้ง)

ทำ 5 ขั้น:
1. เข้า **Extranet** (admin.booking.com) → เมนู **Rates & Availability → Sync calendars**
   (บางบัญชีอยู่ใต้ Calendar → Sync calendars)
2. เลือกห้อง → กด **Export calendar** → คัดลอกลิงก์ `.ics` ที่ได้
3. ทำซ้ำให้ครบทุกห้องที่มีลิงก์แยก (ถ้าระบบให้ลิงก์เดียวรวมทุกห้อง ใช้ลิงก์เดียวพอ)
4. ไปที่ Vercel → Settings → Environment Variables เพิ่มตัวแปรตามตารางล่าง
5. กด **Redeploy** — เสร็จแล้วแบนเนอร์ส้มในหลังบ้านจะหายไปเอง

| Name | Value |
|---|---|
| `BOOKING_ICAL_URLS` | `ห้อง 101\|https://ical.booking.com/...` (หลายห้องคั่นด้วย `,`) |
| `SAFETY_BUFFER` | *(ไม่บังคับ)* จำนวนห้องที่กันไว้ไม่โชว์ว่าว่างบนหน้าเว็บ — ค่าเริ่มต้น `1` (ใส่ `0` ถ้าอยากโชว์ทุกห้อง) |

---

## ขั้นที่ 3 — ใช้งาน

- เปิด **https://houseofhapiness.vercel.app/admin** → ใส่รหัส `ADMIN_PASSWORD`
  (พนักงานใช้ `STAFF_PASSWORD` — หน้าจอเดียวกันแต่ไม่มีส่วนรายได้ มุมซ้ายบนจะขึ้น "โหมดพนักงาน")
- **ผังห้อง**: สีบอกสถานะ (ว่าง / มีผู้พัก / เช็คอินวันนี้ / รอทำความสะอาด)
  แตะห้องเพื่อ **เช็คอิน** (จากการจองหรือ walk-in) / **เช็คเอาต์** / **ย้ายห้อง** / **ทำความสะอาดเสร็จ**
  — ทุกปุ่มบันทึกกลับลงชีตอัตโนมัติ
- รายชื่อห้องอยู่ในชีตแท็บ **Rooms** (สร้างให้อัตโนมัติเป็น 101-115 ครั้งแรกที่ใช้ —
  เปลี่ยนชื่อห้อง/เพิ่ม/ลบได้เองในชีต)
- **สถิติเดือนนี้**: รายได้ตามยอดจอง · คืนที่ขายได้ · อัตราเข้าพัก
- ปฏิทินรายเดือน (แตะวันดูรายชื่อ) + ตารางการจองค้นหาได้
- อยากแก้ข้อมูลละเอียด ๆ → แก้ในชีตได้เลย หน้า /admin อ่านจากชีตสด ๆ ทุกครั้งที่รีเฟรช
- คำขอจองตรงจากหน้าเว็บจะเข้าชีตเป็นสถานะ "รอยืนยัน" อัตโนมัติ

### ติดตั้งเป็นแอปบนมือถือ (PWA)

- **Android (Chrome)**: เปิด /admin → เมนู ⋮ → **เพิ่มไปยังหน้าจอหลัก / ติดตั้งแอป**
- **iPhone (Safari)**: เปิด /admin → ปุ่มแชร์ → **เพิ่มลงในหน้าจอโฮม**
- จะได้ไอคอน "HOH หลังบ้าน" เปิดเต็มจอเหมือนแอปจริง

### คอลัมน์ในชีต

`id` เลขอ้างอิง (BDC-xxx = Booking.com, WEB-xxx = จองตรง) · `source` ช่องทาง ·
`name` ชื่อผู้เข้าพัก · `checkin / checkout` (รูปแบบ `YYYY-MM-DD`) · `nights` จำนวนคืน ·
`guests / rooms / phone / amount` · `status` (ยืนยันแล้ว / รอยืนยัน / ยกเลิก / เช็คเอาต์แล้ว) ·
`note` หมายเหตุ · `created` เวลาที่บันทึก

---

## คำถามที่พบบ่อย

**ถ้า Booking.com เปลี่ยนรูปแบบอีเมลจนอ่านไม่ได้?**
แถวจะขึ้นเป็น "ตรวจสอบเอง" พร้อมหัวข้ออีเมล — เปิดอีเมลแล้วคีย์แก้ในชีตได้เลย
ระบบออกแบบให้ *ไม่มีการจองหายเงียบ ๆ*

**ปลอดภัยแค่ไหน?**
หน้า /admin ต้องใส่รหัสผ่านทุกครั้ง (จำไว้เฉพาะแท็บนั้น), ชีตคุยกับเว็บผ่านรหัสลับ TOKEN,
Google เห็นข้อมูลเท่าที่อยู่ในชีตของบัญชีคุณเอง และหน้า /admin ถูกตั้ง noindex ไม่ให้ติดค้นหา

**ในอนาคตถ้าอยากได้มากกว่านี้ (กันจองซ้อน, ซิงก์ราคา, หลายแพลตฟอร์ม)?**
ค่อยย้ายไป Channel Manager เช่น Beds24 (~$25/เดือน) — ข้อมูลในชีตนี้ export ไปได้
